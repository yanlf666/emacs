

	"include"
	"msp430.h"
	"main(void)"
	"while(1)"
	"defined"
	"elif defined"
	"error"
	
	"pragma"
	"ifndef"
	"define"
	"endif"
	
	"bis_SR_register(LPM0_bits + GIE);
	/* Enter LPM0 interrupt. */
	no_operation();
	/* For debugger */"


	"attribute__ ((interrupt(TIMER0_A0_VECTOR))) Timer0_A0_ISR (void)
	/* Timer0_A0 interrupt service routine */"
	
	"WDTCTL = WDTPW | WDTHOLD;
	/* Stop WDT */"
	
	"PM5CTL0 &= ~LOCKLPM5;
	/* Disable the GPIO power-on default high-impedance mode 
	   to activate previously configured port settings */"

y-base
